解释器的基本实现原理：
	解释器只需从抽象语法树的根节点开始遍历该树直至叶节点，并计算各节点的内容即可。
	
执行程序：
	各个语法树节点对象的类都需要具备eval方法。
	eval方法将计算与以该节点为根的子树对应的语句、表达式及子表达式
	eval方法将递归调用该节点的子节点的eval方法（类似深度优先节点搜索算法）
	不同类型的节点的类，对eval方法有着不同的定义。覆盖
	
环境对象（environment），类似于变量管理器
	一种用于记录变量名称与值的对应关系的数据结构，常以哈希表的形式实现
	当程序中出现新的变量时，由该变量的名称与初始值构成的键值对，将被添加到哈希表之中
	之后再次遇到的时候，程序将搜索哈希表并取出其值。
	如果要赋新值给该变量，程序将会把原有变量的键值对更新为新的数据	

	
